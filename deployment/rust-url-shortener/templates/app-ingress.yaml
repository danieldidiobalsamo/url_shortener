apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-ingress
  namespace: {{ .Values.namespace }}
spec:
  tls: 
  - hosts:
    - url-shortener-rust.com
    secretName: web-ssl
  rules:
  - host: url-shortener-rust.com
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: url-shortener-service
            port: 
              number: 8080